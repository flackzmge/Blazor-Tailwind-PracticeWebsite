@page "/counter"
@using MudBlazor
<!--<h1 class="bg-green-500 hover:text-amber-300 text-lg hover:text-2xl">Hello, world!</h1>
<h1 class="text-4xl font-bold mb-4">Counter</h1>

<p>Current count: @currentCount</p>

<button class="mt-4 bg-gradient-to-br from-green-600 to-green-500 hover:from-green-500 hover:to-green-600 text-white p-2 rounded border border-green-600" @onclick="IncrementCount">Click me</button>
-->
<TextandListBox></TextandListBox>

<!--<br>
<br />
<BlazorChart1></BlazorChart1>

<br>
<br />

<BlazoriseChart1></BlazoriseChart1>-->


<!-- const colors = require('tailwindcss/colors')

module.exports = {
    mode: 'jit',
    purge: [
        './wwwroot/index.html',
        './**/*.razor',
    ],
    darkMode: 'media',
    theme: {
        extend: {
            colors: {
                cyan: colors.cyan
            }
        },
    },
    variants: {
        extend: {},
    },
    plugins: [
        require('tailwindcss-textshadow')
    ]
}-->
@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}